import { FiltroIdadePipe } from './filtro-idade.pipe';

describe('FiltroIdadePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroIdadePipe();
    expect(pipe).toBeTruthy();
  });
});
